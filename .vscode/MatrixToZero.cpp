#include<iostream>
#include<vector>

using namespace std;

vector<vector<int>> matrixtozero(vector<vector<int>> s){
    int m=s.size();
    int n=s[0].size();
    vector<int> m_pos(m,0);
    vector<int> n_pos(n,0);
    /*遍历寻找０*/

    /*数组清零*/
    
    return s;
}